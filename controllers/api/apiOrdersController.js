const db = require('../../database/models/index');


const apisCartsController = {
        list: (req,res)=>{
            res.send('Ok')           
        }         
    };

module.exports = apisCartsController;